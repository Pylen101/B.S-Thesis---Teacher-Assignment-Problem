import sys
import json
import ast


def main():
    result = json.dumps({"name": "John", "age": 30})


if __name__ == '__main__':

#    my_list = json.load(sys.stdin)
#    json.dump(my_list, sys.stdout)
    # my_list = json.load(sys.stdin)
    # json.dump(my_list[0], sys.stdout)
   # data_to_pass_back = "Send this to javascript"
    # input = ast.literal_eval(sys.argv[1])
    # output = input
    # output.append(data_to_pass_back)
    # print(json.dumps(output))
    # sys.stdout.flush()
